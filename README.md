1) Install these packages first depending on your distro 
2) place Hack directory into /usr/share/themes
3) install polybar themes from polybar themes github page then place cuts directory into .config/polybar/
4) place rofi/photon-violet.rasi into /usr/share/rofi/themes/
